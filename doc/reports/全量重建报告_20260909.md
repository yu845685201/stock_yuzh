# 日 K 线全量重建报告（新接口 /api/kline-qfq）

- **执行日期**：2026-09-09
- **执行人**：小伴（自动化任务）
- **触发指令**：打开 `use_enhanced_api` 开关 → 清空 `his_kline_day` 表 → 用新接口全量重建 → 记录耗时

## 1. 关键时间

| 项目 | 数值 |
| --- | --- |
| 开始（CST） | 2026-09-09 17:16:50 |
| 结束（CST） | 2026-09-09 17:35:03 |
| 墙钟总耗时 | **1093 秒 ≈ 18 分 13 秒**（含 TRUNCATE 与收尾） |
| sync 内部墙钟 | 1089.36 秒（sync 自带报告口径） |
| 并发线程 | 8 |
| 股票批次大小 | 1000 |

> 计时来源：`/tmp/rebuild_timing.json`（start_epoch=1788945410，end_epoch=1788946503）与 sync 报告 `总耗时(墙钟): 1089.36 秒` 相互印证。

## 2. 开关与接口路由

- `backend/config/config.yaml` 第 115 行：`kline_day_use_enhanced_api: false` → **`true`**（位于 `sync:` 段）。
- 生效链路：`sync_manager.py:71` 读取 → 注入 `tdx_api_config['use_enhanced_api']` → `TdxApiSource.__init__` 第 25 行生效。链路完整。
- 增强路径映射：
  - qfq 全量/增量 → `/api/kline-qfq`
  - raw 全量 → `/api/kline-all`（未改）
  - raw 增量 → `/api/kline-recent`

## 3. 重建结果

| 指标 | 数值 |
| --- | --- |
| 成功 | 是 |
| 拉取记录数 | 16,435,369 |
| 写库行数 | 16,435,369 |
| 失败股票 | 0 |
| 处理股票 | 5537 只（type='1' 采集池），6 批完成 |
| 除权检测 | 检查 0 / 命中 0 / 整股重拉 0 |
| 数据源缺失 | 前复权空 376 只，原始域空 337 只 |

DB 聚合核对（类型一致查询）：

| 项目 | 数值 |
| --- | --- |
| 总行数 | 16,435,369 |
| 去重股票数 | 5,161 |
| 最小交易日 | 1990-12-19 |
| 最大交易日 | 2026-09-09 |

> 5,161 = 5,537 − 376，与 `qfq_empty=376` 完全吻合：这 376 只源端返回空（多为已退市/未上市/数据缺失），属预期，非漏推。

## 4. 近期日期覆盖（重建后）

| trade_date | 覆盖股票数 |
| --- | --- |
| 2026-09-01 | 5154 |
| 2026-09-02 | 5154 |
| 2026-09-03 | 5155 |
| 2026-09-04 | 5153 |
| 2026-09-07 | 5152 |
| **2026-09-08** | **5152（缺口已修复，重建前仅 3417）** |
| 2026-09-09 | 3742（盘中，源端当日部分就绪，符合预期） |

## 5. 与原接口全量对比

| 维度 | 昨晚（原接口） | 本次（新接口） |
| --- | --- | --- |
| 接口 | `/api/kline` + `/api/kline-all` | `/api/kline-qfq` + `/api/kline-all` |
| 并发 | 8 | 8 |
| 墙钟耗时 | 13 分 54 秒 | **18 分 13 秒** |
| 记录数 | 16,574,970 | 16,435,369 |

> **为何本次更慢**：`init_mode` 全量重建下，`/api/kline-qfq` 需对每只股票首次**冷建 SQLite 缓存**（回源 + 落库），开销高于原 `/api/kline` 直接返回。新接口的性能优势体现在**增量 tail 命中**（约 110ms），而非全量重建。后续日常增量采集将明显快于原接口。

## 6. 结论

1. 全量重建已完成，数据完整（2025 至今全量连续，历史回溯至 1990-12-19）。
2. 9-08 当日缺口已修复（5152 只）。
3. `use_enhanced_api=true` 已生效，日常增量采集将走新接口，性能优于原方案。
4. 原始 67 万行异常数据明细报告已移出仓库（`/tmp/kline_day_sync_report_20260909_173502.md`），需要时可取阅。
