# 环境信息

## 测试环境 (UAT)
- **数据库连接**
  ```yaml
  主机: 127.0.0.1
  端口: 5432
  用户名: postgres
  密码: yuzh1234
  数据库: stock_analysis_uat
  ```
- **数据目录**
  - CSV文件根目录：`uat/data`
  - 通达信数据根目录：`uat/vipdoc`

## 生产环境 (PROD)
- **数据库连接**
  ```yaml
  主机: 127.0.0.1
  端口: 5432
  用户名: postgres
  密码: yuzh1234
  数据库: stock_analysis
  ```
- **数据目录**
  - CSV文件根目录：`prod/data`
  - 通达信数据根目录：`prod/vipdoc`

## 项目目录结构
```
项目根目录/
├── backend/          # 后端代码
│   ├── config/       # 配置文件
│   └── test/         # 单元测试
├── front/            # 前端代码
│   └── test/         # 单元测试
├── doc/              # 正式文档
├── uat/              # 测试环境数据
│   ├── data/         # CSV数据文件根目录
│   └── vipdoc/       # 通达信数据根目录
└── prod/             # 生产环境数据
    ├── data/         # CSV数据文件根目录
    └── vipdoc/       # 通达信数据根目录
```