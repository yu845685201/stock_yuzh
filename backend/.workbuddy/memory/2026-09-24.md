# 2026-09-24 工作日志

## 日K线补齐：his_kline_day 的 2026-09-23（uat 库）

**背景**：09-23 收盘后源端前复权（qfq）整批滞后，当日仅 3480 条，1720 只股票停在 09-22。

**执行过程**：

1. **抽样探测**（30 只 max=20260922 且 L 的股票）：命中率 **30/30 = 100%**，判定源端已更新，可执行采集。
   - 踩坑：`/api/kline-qfq` 返回的日期字段是 **`Time`**（如 `2026-09-23T15:00:00+08:00`），不是 `date`/`trade_date`。首版脚本按 `date` 解析得到"有记录但日期全空"，一度误判为命中率 0%。

2. **首次采集全部失败**：启动后 5537 只全部 `Connection refused`（127.0.0.1:8080），白跑 10 分钟、写库 0 条。
   - 根因：**Docker Desktop 未运行** → 中间件容器 `tdx-stock-web` 停止，8080 无监听。
   - 修复：`open -a Docker`（daemon ~6s 就绪）→ 容器随 restart policy 自动拉起 → 等 `health=healthy`（~30s）→ 接口验证通过。
   - 教训：探测时 8080 正常，1 分钟后开跑就已宕机；**采集启动后必须立刻扫日志确认无 `Connection refused`**。

3. **采集结果**：
   - 第 1 轮（全量 5537 只，--start 20260921 --end 20260923）：7m38s，写库 14510 条，09-23 从 3480 → 约 4959。
   - 第 2 轮（241 只落后，--codes）：11.6s，写库 661 条。
   - 第 3 轮（21 只）：0.65s，写库 7 条。
   - 第 4 轮（7 只源端确有数据）：0.22s，写库 15 条。

4. **最终结果**：`trade_date='20260923'` = **5186 条**（09-21=5188、09-22=5187，基线 5150~5190）→ **已补齐**。
   - 剩余落后 **14 只**，经源端逐只核验为**长期停牌股**（`*ST元道`、`*ST萃华`、`*ST康佳A`、`*ST清越`、`广汽集团`、`信达证券`、`东兴证券` 等），源端 qfq 尾部直接跳过 09-23（形如 `['20260914','20260924']`），属数据源侧正常缺失，非采集问题。
   - 校验：5186 + 14 = 5200 = L 且有 K 线记录的股票总数，完全吻合。

**产出**：采集报告 `stock_yuzh/doc/reports/kline_day_sync_report_20260924_09*.md`（5 份）。

**skill 更新**：`stock-kline-day-backfill` 补充了"中间件整体宕机（Docker Desktop 未启动）的定位与恢复"、"qfq 返回字段名是 `Time`"、"条数统计需等进程完全退出"三个坑，以及"次日早间复测即就绪（30/30）"的实测时点结论。

**未做**：无 CSV 导出；未修改 config.yaml。
