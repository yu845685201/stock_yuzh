# 2026-09-08

## 00:05 启动基本面全量重采（F2）续跑

- 通过后台任务启动 `scripts/resume_fundamentals.sh`，从断点 completed=3662/5537、failed=0 续跑。
- baostock 额度零点刷新后登录恢复正常，跑数正常推进（00:09 已达 3669，failed=0）。
- 剩余约 1875 只，实测速率约 2 只/分钟，估算 10~16 小时跑完，完成后自动执行 G1 验证。

## 01:21 巡检：发现采集中断并重启

- 巡检发现 manager 与 supervisor 进程均已消失，日志自 00:10:40 起停滞 70 分钟（manifest completed=3671、failed=0）。
- 根因：上一轮监督循环在 300 秒 sleep 期间被回收（后台任务会话结束），非 baostock 封禁。
- 已按约定方式（Bash run_in_background=true）重启 `scripts/resume_fundamentals.sh`，进程 38613/38614 存活，日志恢复正常写入。
- 黑名单累计 13 次（集中在 17:36~17:41 封禁窗口），00:05 后登录已恢复；急停仍为 8 次，无新增。
- 有效采集窗口 00:05:58~00:10:40 净增 9 只 → 实测 1.9 只/分钟（sync.fundamentals_rebuild_call_sleep=0.6 生效后）。

## 02:24 巡检：进程再次被回收，重启并验证恢复

- 巡检发现 01:21 那次重启的进程在 01:23:08（上一轮工具会话结束时刻）被回收，manager/supervisor 全灭，停滞 61 分钟。
- 再次用 run_in_background 重启，supervisor 49910、manager 49919；同轮内 sleep 110s 复检确认 3 分钟净增 4 只、日志实时写入。
- 确认后台进程回收发生在「发起它的工具会话结束时」，故巡检重启后必须同轮复检；根治方案是改用 launchd 托管（尚未实施）。
- 进度 3677/5537（66.4%），failed=0；急停 8、黑名单 13 均无新增，未触发限速调整。

## 03:29 巡检：同一回收模式第 3 次复现，重启并复检通过

- 进程全灭、日志自 02:28:26 起停滞 61 分钟，模式与前两轮完全一致（会话结束回收），非 baostock 故障。
- 用 run_in_background 重启，supervisor 52187/52188、manager 52196；同轮 sleep 115s 复检：日志 03:31 有新增、completed 3680→3685（+5），恢复正常。
- 进度 3685/5537（66.6%），failed=0；急停 8、黑名单 13 连续三轮无新增，无需调 fundamentals_rebuild_call_sleep。
- 实测速率约 1.9~2.0 只/分钟，剩余 1852 只 → 约 16 小时，但因每 30 分钟巡检有约 28 分钟空窗，实际完成时间会顺延。
- 建议：改用 launchd 托管 resume_fundamentals.sh 以根治会话回收问题（仍未实施）。

## 05:37 巡检：第 5 次回收复现，已实施 launchd 托管根治

- 04:36:33 后停滞 61 分钟、进程全灭（第 5 次同模式：随发起它的工具会话结束被回收）。completed=3693/5537，failed=0。
- 不再重复 run_in_background 重启，改为根治：新建 `scripts/fundamentals_launchd.sh` 包装器 + `~/Library/LaunchAgents/com.yuzh.fundamentals-rebuild.plist`（RunAtLoad + KeepAlive + ThrottleInterval 30），`launchctl bootstrap` 装载。
- 关键实施细节：装载前先 `pkill` 掉本轮后台实例，避免与 launchd 双开竞争 baostock 会话；包装器内置「等待已有 manager 退出」循环与「完成后 bootout 自卸载」逻辑，防双开且跑完不空转。
- 复检通过：manager 56977 存活（launchd 子进程），3 分钟 completed 3693→3699，日志实时写入。
- 进度 3699/5537（66.8%），failed=0；急停 8、黑名单 13 连续 5 轮无新增，无需调 fundamentals_rebuild_call_sleep。
- 托管后按 1.9 只/分钟连续采集，剩余 1838 只 → 约 16 小时，预计 09-08 21:50 完成。

## 13:20 巡检：F2 收敛期稳定运行，无需干预

- 进度 4445/5537（80.3%），failed=0；launchd 托管跨 4 轮存活（wrapper 61821 / manager 62821，自 10:11 起未变），空窗问题已彻底根治。
- 速率连续 4 轮稳定：12:16→13:20 窗口 1.83 只/分钟、90 秒瞬时 2.0 只/分钟，与 10:11/11:15/12:16 三轮（1.92/1.89）一致。
- 急停 10、黑名单 13 连续 4 轮零新增，call_sleep=0.6 保持不变。剩余 1092 只 → 约 9.9 小时，预计 09-08 23:10 完成。
- 完成后需依次执行：G1 验证（`python3 scripts/verify_g1.py`，除第 [3] 项外全通过）→ M2 日K重建（`bash scripts/rebuild_kline_day.sh`，约 30 分钟，后台运行）。
