# 2026-09-07

## F2 基本面全量重采巡检（自动化任务）
- manifest 统计：completed 3147/5537（56.8%），failed 0；进程存活（pgrep 命中，PID 98117），日志活跃（12:13 仍在写入）。
- 稳定速率约 1.7~1.8 只/分钟，剩余 ~21.7 小时；日志自带的 ETA 字段与实际吞吐不符，不可采信。
- 累计急停 7 次（baostock 会话回收失败 / 探针无数据），黑名单 0 次；非连续，暂不需调 fundamentals_rebuild_call_sleep。
- 已建立自动化巡检基线记录：.workbuddy/memory/automations/2f7ae3bb-c38f-435f-b009-8d47dddc96d9/memory.md

## 17:05 F2 巡检：baostock 黑名单封禁 + 配置位置修复
- 进度停在 completed 3662/5537（66.1%），failed 0；自 16:41:07 起 manifest 不再更新。
- 根因：baostock 返回「黑名单用户，请与管理员联系」，连续 6 次重启登录均失败（16:41/16:46/16:51/16:56/17:01/17:06）。
- **配置坑（重要）**：`fundamentals_rebuild_call_sleep` / `fundamentals_rebuild_recycle_calls` 原先写在 config/config.yaml 的 `csv:` 段下，而代码读取路径为 `sync.*`（src/sync/fundamentals_rebuild_manager.py:52-53），导致这两项防封禁配置**从未生效**，一直使用默认值 0.3 / 300。
- 处置：将两项配置移入 `sync:` 段并上调为 call_sleep 0.6、recycle_calls 1000（降低重登录频率），已验证 `sync.` 路径可读到新值。
- 未重启采集：监督循环 resume_fundamentals.sh（PID 98109）存活且剩约 34 轮 × 5 分钟重试，新配置下一轮自动生效；此时重启会双开加剧封禁。
