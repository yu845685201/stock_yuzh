# 2026-09-08

## 08:52 进度巡检与故障处置
- 断点：completed=3831/5537（69.2%），failed=0。
- 发现：9/8 00:05 由 automation 启动的采集，8.8 小时只完成 134 只（速率 0.32 只/分钟，昨日为 1.9~3.2）。诊断结论——**不是额度问题**（今日累计仅 7,121 次调用，距 5 万还剩 42,879），而是**进程被反复回收**：日志里有 04:33 / 05:38 / 05:42 三次重启，大部分时间根本没在跑。
- 印证了「由 automation 会话启动的后台进程活不久」的担忧。

## 处置：改用 setsid 脱离会话启动
- 先杀掉空转 supervisor（PID 56972），再用 **双 fork + os.setsid()** 启动 resume_fundamentals.sh，让孙进程被 init 收养，脱离任何会话。新实例 supervisor=61855 / runner=61860。
- 恢复后实测速率 **1.93 只/分钟**，5 分钟净增 9 只，正常。
- ⚠️ 踩坑：脚本防双开用 `pgrep -f "fundamentals_rebuild_manager"`，**会匹配到调用方的 shell 命令行**——只要启动命令里含该字符串就会误判「已有进程在运行」并拒绝启动。启动命令必须避开这个字符串（第一次尝试因此失败）。

## 额度测算（重要）
- 实测每只股票约 **44~46 次调用**。
- 剩余 1,693 只 × 46 ≈ **7.8 万次调用**，而 9/8 当日只剩约 4.2 万次额度 → **今天跑不完，需跨到 9/9**。
- 今天预计能推进约 900~950 只，剩余约 750 只留到 9/9。

## 09:02 网络中断与 IP 变更
- 用户本机断网、IP 变更。日志证据是 `Broken pipe` / `接收数据异常` / `Baostock登录失败：网络接收错误` —— **属网络故障，不是 baostock 封禁**（封禁症状是「黑名单用户，请与管理员联系」，全程未出现）。
- 用户据此判断：IP 变更后 baostock 的 5 万次/日额度重新计满。
- 09:07 验证：新 IP 下 baostock 登录与采集正常（出现正常的「会话回收（已 300 次调用），重新登录」），速率 1.56 只/分钟，completed 3844→3851。
- 额度再测算：剩余约 1,686 只 × 46 次 ≈ 7.8 万次 > 5 万，**即便额度重置今天也跑不完**，今日约能推进 1,087 只（到 completed≈4,938），剩余约 600 只需 9/9。

## 16:36 触发黑名单，用户要求中断
- 证据：日志 184383 行等多次 `Baostock登录失败: 黑名单用户，请与管理员联系`（今日 API 调用超 5 万）。
- 已停：supervisor 61855 + 被拉起的两个子进程（84141/84147）；并把整点巡检 automation(2f7ae3bb…) 置为 **PAUSED**，防止自动重启再次触发调用。
- 断点快照：completed=**5015/5537（90.6%）**，failed=0；manifest 367KB（mtime 16:21）；DB **244,866 行 / 4,943 只**；剩余 **522 只**，预计需约 2.4 万次调用（明天单日额度足够）。
- 今日效率参考：09:03(3844) → 16:21(5015)，7.3 小时完成 1,171 只 ≈ **160 只/小时**。按此，剩余 522 只明天约 3~4 小时可跑完。
- 9/9 恢复方式：用双 fork + os.setsid() 方式启动 resume_fundamentals.sh；启动命令勿含 `fundamentals_rebuild_manager` 字样（防双开误判）。

## 19:04 换 IP 后恢复采集（收尾跑）
- 先探活：`bs.login()` + 取数均返回 error_code=0 success，确认黑名单已解除再开跑（macOS 无 `timeout` 命令，别用）。
- 用双 fork + os.setsid() 启动，正确接管断点「manifest 已完成 5015，跳过」。
- 5 分钟实测：5016→5027（+11），速率 **2.36 只/分钟（141 只/小时）**，剩余 510 只，预计 **约 3.6 小时**（约 22:45 完成），之后自动进 G1。
- 核实过：本轮日志内黑名单/急停/登录失败均为 0（此前 tail -500 看到的 13 次是 16:21 前的历史残留，非新发生）。
- 整点巡检 automation 仍保持 PAUSED，避免多实例干扰；需要看进度叫我即可。

## 20:11 用户回家，主动中断
- 已停全部进程（84377 supervisor + 92322/92332/98416 等子进程），断点落盘 completed=**5236/5537（94.6%）**，failed=0；manifest 383KB（mtime 20:11:27）；DB **249,168 行 / 5,164 只**。
- 剩余 **301 只**，预计约 1.4 万次调用 —— 一次就能跑完（额度充裕），按今晚 195 只/小时的速率约 1.5 小时。
- 回家后恢复方式：双 fork + os.setsid() 启动 resume_fundamentals.sh（启动命令勿含 `fundamentals_rebuild_manager` 字样）；若期间 IP 再变，先跑 bs.login() 探活再开。

## 21:47 用户授权：F2 跑完后自动推进 G1→M2→G3
- 前置检查全部通过：baostock 探活 OK（换 IP 后可用）；tdx-api 中间件 healthy（容器 Up 8 weeks）；G3 锚点 `tmp/baseline_*.json` 在（9/5 生成）；his_kline_day 当前 16,652,712 行（与手册 1,665 万锚点吻合）。
- 已用 setsid 重启 F2（接管断点 5241），并新建编排脚本 `/tmp/orchestrate_f2_to_m2.sh`，后台任务 id=3QckHx，日志 `/tmp/orchestrate_20260908_214723.log`。
- 编排逻辑：轮询等待 F2 达标（completed≥5537 且 failed=0）**且采集进程退出** → 解析 /tmp/fundamentals_supervised.log 中最后一次 G1 结论 → **仅当失败项只有「前视」时才放行**（符合手册 §3.2，前视项 M2 后才生效）→ 执行 `bash scripts/rebuild_kline_day.sh`（TRUNCATE + 全量重建，脚本末尾自动跑 G3）。
- 安全阀：G1 若有非前视项失败则**自动终止、不执行 M2**（退出码 2）；F2 超 8 小时未完成也终止（退出码 1）。
- 注意 verify_g1.py 的失败标记是 Unicode `✗`（U+2717），脚本判定用 `\u2717`，勿写成 'X'。

## ✅ 22:22 F2 全部完成 + G1 结果 + M2 放行决策
- **F2 完成：completed=5537/5537，failed=0**（DONE 标记未生成，但 manifest 达标即完成）。DB 252,878 行 / 5,464 只。
- G1 六项：✓stat_date 非空 ✓覆盖 5464≥5000 ✓披露日≥报告期 ✓前视=0 ✓平安银行 pubDate(20251025) **✗turnover 抽样一致(2/20)**。编排脚本按安全阀拦截（exit 2，未自动跑 M2）。
- **turnover 失败根因已查明（关键，勿重复排查）**：偏差全部集中在 sh.600373 的 `disclosure_date=20160331` 这批（61 行全偏 6.19%），其 float_share 由 11.86 亿(2016) 变为 13.55 亿(2021)；而同股 `20210331` 那批 60 行零偏差，另两只抽样股（600017/600375）也是 0 偏差。
  → 即**旧日K的 turnover_rate 用当年股本算，基本面重采后股本更新才对不上**，属 M2 重建要修复的对象，非基本面数据错误。
- 决策：依据手册 §3.2（turnover 抽样「M2 重建后才可验」）+ 其余五项全过，判定可放行 M2。22:30 已启动 `rebuild_kline_day.sh`（后台任务 uRxihz），TRUNCATE 16,652,712 行后全量重建，脚本末尾自动跑 G3。

## ✅ 22:39 M2 完成 + G3 全部通过 + G1 复验全绿（全流程收官）
- M2：`success: True`，records/db_rows **16,574,970**，failed_stocks **0**；detection checked=0（全量重建无需跨快照检测）；source_missing: qfq_empty=351 / raw_empty=337（少量股票无源数据，属正常）。耗时 13m54s。
- G3 八项全过：行数在基线 0.9~1.3 倍内、amount 零值≈0、raw_close 覆盖>99%、adjust_flag 全=2、前视=0、抽样 30 只行数≥基线 98%、sz.000001 与 sh.600519 除权日 preclose 衔接。
- **G1 复验：turnover 抽样由 2/20 不一致 → 0/20 全一致，G1 六项全部通过**。证实此前「turnover 偏差是旧股本遗留、M2 重算即修复」的判断成立。
- 至此 F2 → G1 → M2 → G3 全部完成。
- **提交已推送**：commit `5045da3`（53 文件 / +705,696 行）→ `origin/feature/data_coll_simple`（5f4735e..5045da3）。暂存时按手册排除了 backend/test.py、test_baostock.py、backend/tmp/。
- 完成报告：`doc/reports/全量跑数完成报告_20260908.md`（该文件尚未提交）。
- 下一步：新分支 feature/data_coll_opt_260905 → Phase 3（XDXR spike → /api/kline-recent → qfq 缓存 → 周度校准 → 部署 → M-C 开关）。

## 22:50 日K完整性复查 + 补采遗漏 14 只（重要）
- M2 报 failed_stocks=0，但日志里 `source_missing: qfq_empty=351 / raw_empty=337` 需核实。复查发现：
  - 有日K的 5,186 只**全部是 type=1（A股）**，无杂类混入 ✓
  - base_stock_info 共 8,852 只，无日K的 3,666 只中 type=2/4/5 有 3,315 只（非A股，本就不在采集范围）
  - **A股缺口 351 只 = 337 退市(D) + 14 在市(L)**
- 验证 14 只在市股：直接查中间件 `/api/kline?code=688282` **有数据（Count 1085）** → 证明是采集遗漏而非源缺失。
- 补采：`python3 -m src.cli.main sync-kline-day --codes <列表> --init`（单只 0.9 秒；350 只批量耗时 146 秒，写入 26,105 条）。
- 结果：日K表 **16,602,160 行 / 5,200 只**；**在市 A 股 100% 覆盖**（14 只全补齐）；仍缺 337 只退市股（tdx 数据源不再提供已退市个股数据，属客观限制，非采集失败）。
- 补采后重跑 G3：**8/8 仍全部通过**。
- 经验：`failed_stocks=0` 不等于无遗漏，`source_missing.qfq_empty` 才是真正要看的缺口指标；M2 后应核对「base_stock_info 中 type=1 且 list_status='L' 是否都有日K」。
- 补采产生的新报告文件（doc/reports/kline_day_sync_report_20260908_*.md）尚未提交。

## 23:00 提交报告 + main 合并（本地完成，推送受阻）
- 提交 `0f8f3db`（3 个报告文件，+328 行）到 feature/data_coll_simple 并推送成功。
- 本地已完成：`git checkout main` → `git merge feature/data_coll_simple --no-ff`（合并提交）。
- ⚠️ **远程 main 尚未更新**：推送报 `CONNECT tunnel failed, 502`。原因：环境变量 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:64367` 指向的本地代理已失效（64367/7890/1087/8888 全部不可连接，直连 GitHub 也超时）。22:41 那次推送成功时代理还在。
- 恢复方式：启动代理软件后执行 `cd .../stock_yuzh && git push origin main`（当前已在 main 分支）。

## 22:56 Phase 3（tdx-api 优化）环境勘察
- 仓库 `/Users/yuzh/develop/ai/claude/claude-code/workspace/test/tdx-api`，分支 **main**，远程 oficcejo/tdx-api，最新提交 3a84e44。
- 环境：Go 1.17.3（/Users/yuzh/develop/go/bin/go）✓；Docker 容器 tdx-stock-web Up 8 weeks (healthy) ✓。
- ⚠️ **仓库有 7 个未提交变更**：API_使用示例.py(+128/-31)、client.go(+4)、codes.go(+6/-3)、docker-compose.yml(+4/-4)、codes.db、workday.db、以及未跟踪的 `web/data/database/kline/`（含 sh600000.db，1.1MB）。动手前须先厘清/提交，避免与 Phase 3 改动混杂。
- 手册路径修正：`model_kline.go` 实际在 **`protocol/model_kline.go`**（手册只写 model_kline.go）。其余关键文件均在：protocol/const.go(70行)、web/server_api_extended.go(1295行)、web/server.go(747行)、client.go(801行)。
- `web/data/database/kline/` 已存在且已有 per-code SQLite 文件，M-B 需在其中做「qfq 与原始域分表」。

## runner 行为观察
- 每轮跑约 5 分钟（约 569 次调用 / 13 只）后即以 blocked=True 退出，supervisor 等 300 秒再启动下一轮，**有效效率约 50%**（一半时间在等重启）。这是整体速率上不去的结构性原因。
