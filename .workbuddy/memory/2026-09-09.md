# 2026-09-09

## 09:12 用户担心「来回 merge 导致优化代码被覆盖」，完成核查
结论：**代码完整，无覆盖丢失**。证据：
- `git diff feature/data_coll_simple main` 为空，两分支内容完全一致。
- 5045da3（跑数）、0f8f3db（报告）、e8cdb8e（merge）、74a6c72 全部是 main 的祖先。
- 14 个单测齐全（test_kline_day_merge.py 8 个 + test_fundamentals_mapping.py 6 个）。
- 关键文件均在：sync_manager.py(1713)、fundamentals_rebuild_manager.py(340)、tdx_api_source.py(163)、baostock_source.py(404)、connection.py(1460)；`BaostockBlacklistError` 类存在。

## 分支操作事实澄清
- 我（本会话）这段时间**没有做任何代码优化**，只做跑数（F2/G1/M2/G3/补采）+ 2 次提交 + 1 次 merge。
- 全部操作在 **feature/data_coll_simple**；对 main 的唯一动作是 09-08 23:01 的本地 merge，**未在 main 上直接编辑任何文件**。
- 09-09 09:01 的 `74a6c72`「优化日k线数据采集逻辑…避免并发进入黑名单」**不是我做的**，且其实际内容只有 memory 记录、`backend/tmp/` 数据、`backend/test.py`、`backend/test_baostock.py`、`.zcode/plan`——**无任何代码文件，提交消息与实际不符**。

## 发现的两个问题（待用户处理）
1. `74a6c72` 把手册 §4.1 明确要求排除的文件提交了：`backend/tmp/`（含 baseline_*.json、fundamentals_rebuild_manifest.json，+325,315 行）、`backend/test.py`、`backend/test_baostock.py`。建议后续清理或加 .gitignore。
2. `config.yaml` 的 `fundamentals_rebuild_call_sleep` 实际是 **0.6**、`fundamentals_rebuild_recycle_calls` 是 **1000**（来自 5045da3，即接手时工作区就是此值），与手册正文写的 0.25 / 300 不符。手册说法需订正，否则后续调参会被误导。

## 09:26 修正 .gitignore，排除 backend/tmp（已完成并推送）
- 在根目录 `.gitignore` 末尾追加 `backend/tmp/`（带注释说明：断点 manifest、G3 基线锚点本地保留但不入库）。
- `git rm -r --cached backend/tmp/` 摘除已误入库的 6 个文件（**-324,735 行**），本地 7.7MB 文件全部保留，baseline_*.json 与 manifest 均完好（G3 对账不受影响）。
- 提交 `db2c3bb` 并成功推送 `2f95fb4..db2c3bb main -> main`（此前 23:00 的 502 已恢复）。
- ⚠️ 仍未处理：手册 §4.1 要求排除的 `backend/test.py`、`backend/test_baostock.py` 也已随 74a6c72 入库，待用户决定是否一并排除。

## 09:30 ① 测试脚本归位 ② 手册按实际代码修正（均已提交推送）
### ① 测试脚本统一到 backend/test/
- `git mv backend/test.py backend/test/manual_check_kline.py`、`backend/test_baostock.py backend/test/manual_check_baostock.py`。
- **关键点：重命名为 `manual_*` 前缀**。这两个是顶层直接执行的脚本（含 `bs.login()` 网络调用），若保留 `test_*.py` 命名会被 pytest 自动收集，可能拖垮现有 14 个单测。改后 `pytest test/` 收集 14 个、**14 passed**，未误收 manual 脚本。
- backend 根目录剩余 .py 均为入口/工具脚本（main.py、run_cli.py、setup.py、fix_db_schema.py），非测试文件。
- 提交 `81fcdd4`。

### ② 手册（doc/全量跑数执行手册.md）修正，提交 `c2a1e70`，推送 `db2c3bb..c2a1e70`
- **防封禁参数**：300/0.25 → 实际 `recycle_calls: 1000`、`call_sleep: 0.6`（并标注代码兜底默认 300/0.3，注明旧值作废）。
- **新增 §5.1 每日 5 万次上限**（手册此前完全缺失）：总调用 25.5 万次、须跨 5~6 天、与网络故障的区分、换 IP 可重置。
- **G1 验收**：由「除 [3] 外全过」改为「除 [3]、[6] 外全过」，并写明 turnover 项 M2 前失败属预期（含可放行/需排查的判定特征与 sh.600373 实例）。
- **§3.1 失败处置**：call_sleep 已是 0.6，改为「上调至 1.0 + recycle 下调至 500」。
- **新增 §5.19 长跑进程启动方式**（setsid 双 fork；命令行禁含 `fundamentals_rebuild_manager`；ps 被禁用改用 pgrep）。
- **新增 §5.20**：`failed_stocks=0` 不等于零遗漏，要看 `source_missing.qfq_empty`。
- **§4.1 排除项**：test.py/test_baostock.py 已归位改为纳入；tmp/ 注明由 .gitignore 排除。
- M2 耗时 30 分钟 → 实测 14 分钟；F2 调用量写明 254,710。

## 09:50 tdx-api git pull 影响评估（用户已备份 + 手动删 index.lock）
- 远程 origin/main **领先本地 3 个提交**：
  - `ea07dcc` fix: 交易日判定 / kline-history 日期参数 / ETF 价格三位小数 / K线 Amount 为 0 / 图表挤压
  - `e16e751` ci: 添加 Docker 镜像构建流水线（GHCR）
  - `200fc32` fix: 涨速符号（ReversedBytes9 改 int16）
- 远程改动 8 文件（+217/-13）：.dockerignore、.github/workflows/docker-publish.yml、protocol/model_quote.go、protocol/unit.go、**web/server.go(+63)**、**web/server_api_extended.go(+50)**、web/static/app.js、workday.go。
- **风险评估：低**。① 本地 main 是 origin/main 祖先，可 fast-forward 无分叉；② 远程改动文件与本地 7 项未提交改动**零重叠**，pull 不会覆盖 client.go/codes.go/docker-compose.yml 等本地成果。
- ⚠️ **对 Phase 3 的影响（重要）**：手册里的行号锚点会失效。
  - `getQfqKlineDay`：手册写 168 行，实际旧版在 **176** 行（hunk `@@ -176,6 +181,15 @@`），pull 后变 **181** 行。
  - M-A 注册位置：手册写 `web/server.go:712-740`，远程在 645 行后 +43 行，pull 后约 **755-783**。
  - → Phase 3 开工前须按**函数名**重新定位，勿再引用手册行号。
- pull 只改工作区，**不影响运行中的容器**（需 docker compose build 才生效）。
- 潜在收益：`K线 Amount 为 0` 修复可能改善 G3 的 amount_zero（当前 161 条）。

## git 状态备忘
- main = 2f95fb4（PR #2 merge），feature = 75d160a（PR #1 merge），均与 origin 同步，远程已更新。
- 昨晚 23:00 推送曾因本地代理 `127.0.0.1:64367` 失效报 502，今早已随 PR 合并一并解决。

## tdx-api 双远程仓库配置（test/tdx-api）
- 完成 origin(自有)→ git@github.com:yu845685201/tdx-api-yuzh.git，upstream(原作者)→ git@github.com:oficcejo/tdx-api.git，main 已推送并跟踪 origin/main。
- 坑1：本地是浅克隆导致 push 报 did not receive expected object，需 `git fetch --unshallow upstream` 补全。
- 坑2：HTTPS 走 Clash 代理报 502，git 操作全部改用 SSH。
- 坑3：.git/config.lock 残留导致 remote 命令失败，确认无 git 进程后删除即可。
- 同步上游：git fetch upstream && git merge upstream/main && git push。

## 12:22 用户确认 Phase 3 为下一阶段，要求先核对文档
- 用户记忆：跑完基本面(F2)+日K(M2)后该对 tdx-api 做优化。已核对 `doc/全量跑数执行手册.md` §4.3 与 `doc/reports/全量跑数完成报告_20260908.md`「下一步」，确认无误。
- 当前状态：F2/G1/M2/G3 已于 2026-09-08 全跑通（已推送 5045da3），处于「提交 → Phase 3」关口；§4.1 提交已完成，待 §4.2 新分支 `feature/data_coll_opt_260905` 与 §4.3 Phase 3。
- Phase 3 内容（tdx-api 仓库内）：XDXR spike → M-A /api/kline-recent → M-B qfq 缓存(+M-B3 周度校准) → 部署 → M-C use_enhanced_api 开关切换。
- ⚠️ 开工前必做：因 09:50 的 `git pull upstream/main` 改动了 web/server.go、web/server_api_extended.go（Phase 3 要改的同批文件），手册行号锚点已失效，必须按函数名（getQfqKlineDay、M-A 注册位）重新定位，勿引行号。

## 14:10 整体规划梳理（用户要求：F2+M2 完成后重新规划后续）
核实两库实际状态后给出四阶段规划：
- **① Phase 3 tdx-api 增强**（主线，手册 §4.3）：新分支 feature/data_coll_opt_260905 → XDXR spike → M-A → M-B+M-B3 → 部署 → M-C。
- **② PROD 全量跑数**（与①并行）：⚠️ 新发现——PROD 库基本为空（base_stock_info 仅 500 行）且**缺 base_trade_calendar / base_adjust_factor_info 两张表（schema 漂移）**，须先用 init-V1.sql 对齐 schema + 生成 PROD 基线快照，再走 F2→G1→M2→G3。
- **③ 数据缺口补齐**：复权因子两环境均 0 行（设计有、从未采集）；UAT 1min 分区停在 2026-04-10（147 分区，落后 5 个月）；立体K线最后生成 2026-03-21。指数/周K/月K 未建设（PROD 已有空表），是否纳入待用户决策。
- **④ 运维自动化**：日K 日增量 + M-B3 周度校准挂定时；基本面季度增量（4/8/10 月）。
- 关键约束：②的 F2 与复权因子采集共享同 IP baostock 日限 5 万次，须统一排预算；①③（1min）走 tdx-api 不占额度。
- 已核实的 UAT 数据锚点：基本面 252,878 行/5,464 只；日K 16,602,160 行/5,200 只，最新交易日 20260908。
- 建议下一步：先启动①（建分支+XDXR spike），并行做②的 schema 对齐+基线快照让 PROD F2 当天开跑。等用户确认动手点。

## 14:27 用户决策：精简待办 + 统一分支名
- **移除待办**：复权因子（base_adjust_factor_info）采集、PROD 全量跑数，均不再考虑。PROD schema 漂移、基线快照等事项随之作废。
- **分支定名**：tdx-api 与 stock_yuzh 两个工程的开发分支统一为 `feature/opt_0909`（替代手册原计划的 feature/data_coll_opt_260905）。
- 核实结果：两仓库的 feature/opt_0909 均已存在——stock_yuzh 在 cd13de1（=main）已与 origin 同步并已切换；tdx-api 在该分支上，有 1 个本地提交 233d371（chore: gitignore/停止跟踪运行时文件）**未推送**到 origin，已告知用户待其决定。
- 手册 §4.2 已同步改为 feature/opt_0909 并注明两工程统一分支名。
- **精简后待办**：① Phase 3 tdx-api 增强（XDXR spike → M-A → M-B+M-B3 → 部署 → M-C）；③ 1min 追平（UAT 分区停在 2026-04-10）+ 立体K线追平；④ 运维自动化（日K 日增量、M-B3 周度校准、基本面季度增量）。

## 14:35 tdx-api 优化技术方案已产出（doc/tdx-api中间件优化技术方案-V1.0.md）
- 基于 V3.0 §3/§7 + 当前代码核实（feature/opt_0909，含 upstream 3 提交）写成施工设计：T1 XDXR spike（1-2天，唯一不确定点）→ T2 M-A kline-recent（0.5天）→ T3 M-B qfq缓存+XDXR失效+refresh旁路（2-3天）→ T4 周度校准 → T5 部署 → T6 M-C 开关切换+灰度（≤8分钟验收）。
- **代码事实锚点**（函数名）：getQfqKlineDay 在 web/server.go（5 处调用：handleGetKline/handleGetKlineHistory 的 day/week/month 分支 + kline-all/ths 两处）；路由注册在 server.go 的 HandleFunc 段；GetKlineDay(code,0,N) 单请求签名已实证；KMSG_XDXRINFO=0x000f 在 const.go 注释块待提升为 TypeXdxr；分发 switch 在 client.go handlerDealMessage。
- **pytdx 协议疑点**：XDXR 响应日期字段笔记写"9字节模式"，pytdx 源码实为 4 字节 uint32 LE（YYYYMMDD 整型），spike 首验并回写笔记。
- **新发现的部署坑（高优先级）**：docker-compose.yml 卷源路径仍指向已不存在的 test/tdx-api，当前容器靠 bind mount inode 存活（已实测容器内 /app/data/database 与新位置一致），down/重建即丢挂载——T5 第一步必须先修卷路径。
- **本地 kline 缓存库现状**：web/data/database/kline 仅 sh600000.db，基本为空，M-B 需懒加载+首夜批量预热。
- 方案排期 D1-D8；文档尚未提交 git，待用户评审。

## 14:47 方案修订 V1.1：确立"只新增、不修改"红线（用户两问驱动）
- 用户问：接口能否只新增不修改？代码能否只新增不修改？答案：都能，方案已修订。
- **接口只新增**：不再改造 /api/kline、/api/kline-history 行为，缓存能力改由**新接口 `/api/kline-qfq`**（type=day|week|month，带 refresh=1 旁路）提供；旧接口零变更，兼作周度校准的权威对照源（校准流程反而简化：新接口缓存值 vs 旧接口回源值比对，不一致再 refresh=1 修正）。
- **代码近零修改**：`getQfqKlineDay` 完全不动（含签名），作为新缓存模块（新文件 web/qfq_cache.go）的回源函数被调用；对既有文件仅 3 处纯新增行：client.go 分发 case ×1、const.go 常量 ×1（也可放新文件）、server.go 路由注册 ×2。唯一真正的修改是 docker-compose.yml 卷路径（修既有 bug）。
- 文档已改名 doc/tdx-api中间件优化技术方案-V1.1.md，相关章节（§1 红线说明、§3 总设、§4.1/4.3/4.4/4.6、§5 契约、§7 风险）全部同步。
- 代价（已向用户明示）：web 前端等旧接口调用方不享受提速；新旧接口长期并存，stock_yuzh 用 use_enhanced_api 开关选路径，回退与现状完全等价。

## 15:09 用户三问：上游更新对新接口的影响 → 方案补 §8
- 结论：冲突面极小且全部显式（新文件零冲突，3 处新增行机械冲突）；上游协议层/getQfqKlineDay/THS spider 修复**自动继承**（调用关系红利）；getQfqKlineDay 签名变更或上游自带 XDXR → 编译错误显式暴露。
- 两个需机制保障的点：①"旧接口=权威对照源"假设（上游若给旧接口加缓存→校准静默失灵，列为合并后必查项）；②存量缓存版本漂移（QfqMeta 加 CacheVersion 字段，版本不符自动重建）。
- 方案 V1.1 新增 §8（上游同步影响分析与保障机制：关注清单/合并后回归/缓存版本号/周度校准兜底），QfqMeta 表结构已补 CacheVersion 字段；TypeXdxr 常量定放新文件 model_xdxr.go（const.go 零触碰）。

## 15:10-16:15 Phase 3 全部落地（T1-T5 完成，T6 已交付待全量验收）
- **T1 XDXR spike ✅**：protocol/model_xdxr.go（新）+ client.go 分发 case + GetXdxr。与 pytdx 对照 4 股 288 条事件逐条一致，~35ms。修正笔记：日期是 4 字节 uint32 LE（pytdx get_datetime(9) 的 9 是模式参数非字节数），记录 29 字节。
- **T2 /api/kline-recent ✅**：43ms vs kline-all 485ms。
- **T3 /api/kline-qfq ✅**：web/qfq_cache.go（新），缓存命中 ~110ms vs 回源 620ms；与旧接口权威值零差异；锚定日回退模拟除权→自动失效回源 ✔；refresh=1 旁路 ✔。性能要点：XdxrEvent 持久化必须单事务（单条 INSERT 各自 fsync 导致 220ms→110ms）。
- **T4 校准脚本 ✅**：scripts/weekly_qfq_calibrate.py，污染→检测→自动修正链路通。
- **T5 部署 ✅**：docker-compose 卷路径已修（旧 test/tdx-api 路径失效，容器靠 inode 存活）；本机网络拉不了 golang 构建镜像，用**离线分层镜像**（FROM 本地旧镜像 + COPY 本地交叉编译 linux/arm64 二进制）部署；生产容器已重建回归通过。技术验证套路：docker run 旧镜像挂 8081 → docker cp 新二进制 → restart。
- **T6 🔄**：stock_yuzh 侧 use_enhanced_api 开关已交付（tdx_api_source 四方法分支 + sync_manager 注入 + 检测命中重拉 refresh=True），默认 false。15 股双路径对账零差异、2 股端到端 0.18s/股。待办：100 股对账、首夜缓存预热、全市场增量验收（预期 2-3 分钟）、验收后翻 true。
- 提交：tdx-api cfbff9c、stock_yuzh 5a91c85 + 79f67df（完成报告 doc/reports/Phase3完成报告_20260909.md），均已推送。
- **Go 工具链**：本地装 go1.23.4 于 ~/.workbuddy/binaries/go；构建需 GOPROXY=https://goproxy.cn,direct GOSUMDB=off GOFLAGS=-mod=mod（本机代理拦 proxy.golang.org）。web/go.mod 会被 -mod=mod 自动改（uuid 转 direct），提交前 git checkout 还原。
- **curl 注意**：本机 http_proxy 会拦截 127.0.0.1 请求，必须加 --noproxy '*'。
- 观察项：600519 当日 bar THS 与 TDX 两源 close 差 1.29 元（THS 收盘后修订），既有问题非本次引入，周度校准兜底。
