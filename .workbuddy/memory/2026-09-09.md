# 2026-09-09

## 09:12 用户担心「来回 merge 导致优化代码被覆盖」，完成核查
结论：**代码完整，无覆盖丢失**。证据：
- `git diff feature/data_coll_simple main` 为空，两分支内容完全一致。
- 5045da3（跑数）、0f8f3db（报告）、e8cdb8e（merge）、74a6c72 全部是 main 的祖先。
- 14 个单测齐全（test_kline_day_merge.py 8 个 + test_fundamentals_mapping.py 6 个）。
- 关键文件均在：sync_manager.py(1713)、fundamentals_rebuild_manager.py(340)、tdx_api_source.py(163)、baostock_source.py(404)、connection.py(1460)；`BaostockBlacklistError` 类存在。

## 分支操作事实澄清
- 我（本会话）这段时间**没有做任何代码优化**，只做跑数（F2/G1/M2/G3/补采）+ 2 次提交 + 1 次 merge。
- 全部操作在 **feature/data_coll_simple**；对 main 的唯一动作是 09-08 23:01 的本地 merge，**未在 main 上直接编辑任何文件**。
- 09-09 09:01 的 `74a6c72`「优化日k线数据采集逻辑…避免并发进入黑名单」**不是我做的**，且其实际内容只有 memory 记录、`backend/tmp/` 数据、`backend/test.py`、`backend/test_baostock.py`、`.zcode/plan`——**无任何代码文件，提交消息与实际不符**。

## 发现的两个问题（待用户处理）
1. `74a6c72` 把手册 §4.1 明确要求排除的文件提交了：`backend/tmp/`（含 baseline_*.json、fundamentals_rebuild_manifest.json，+325,315 行）、`backend/test.py`、`backend/test_baostock.py`。建议后续清理或加 .gitignore。
2. `config.yaml` 的 `fundamentals_rebuild_call_sleep` 实际是 **0.6**、`fundamentals_rebuild_recycle_calls` 是 **1000**（来自 5045da3，即接手时工作区就是此值），与手册正文写的 0.25 / 300 不符。手册说法需订正，否则后续调参会被误导。

## 09:26 修正 .gitignore，排除 backend/tmp（已完成并推送）
- 在根目录 `.gitignore` 末尾追加 `backend/tmp/`（带注释说明：断点 manifest、G3 基线锚点本地保留但不入库）。
- `git rm -r --cached backend/tmp/` 摘除已误入库的 6 个文件（**-324,735 行**），本地 7.7MB 文件全部保留，baseline_*.json 与 manifest 均完好（G3 对账不受影响）。
- 提交 `db2c3bb` 并成功推送 `2f95fb4..db2c3bb main -> main`（此前 23:00 的 502 已恢复）。
- ⚠️ 仍未处理：手册 §4.1 要求排除的 `backend/test.py`、`backend/test_baostock.py` 也已随 74a6c72 入库，待用户决定是否一并排除。

## 09:30 ① 测试脚本归位 ② 手册按实际代码修正（均已提交推送）
### ① 测试脚本统一到 backend/test/
- `git mv backend/test.py backend/test/manual_check_kline.py`、`backend/test_baostock.py backend/test/manual_check_baostock.py`。
- **关键点：重命名为 `manual_*` 前缀**。这两个是顶层直接执行的脚本（含 `bs.login()` 网络调用），若保留 `test_*.py` 命名会被 pytest 自动收集，可能拖垮现有 14 个单测。改后 `pytest test/` 收集 14 个、**14 passed**，未误收 manual 脚本。
- backend 根目录剩余 .py 均为入口/工具脚本（main.py、run_cli.py、setup.py、fix_db_schema.py），非测试文件。
- 提交 `81fcdd4`。

### ② 手册（doc/全量跑数执行手册.md）修正，提交 `c2a1e70`，推送 `db2c3bb..c2a1e70`
- **防封禁参数**：300/0.25 → 实际 `recycle_calls: 1000`、`call_sleep: 0.6`（并标注代码兜底默认 300/0.3，注明旧值作废）。
- **新增 §5.1 每日 5 万次上限**（手册此前完全缺失）：总调用 25.5 万次、须跨 5~6 天、与网络故障的区分、换 IP 可重置。
- **G1 验收**：由「除 [3] 外全过」改为「除 [3]、[6] 外全过」，并写明 turnover 项 M2 前失败属预期（含可放行/需排查的判定特征与 sh.600373 实例）。
- **§3.1 失败处置**：call_sleep 已是 0.6，改为「上调至 1.0 + recycle 下调至 500」。
- **新增 §5.19 长跑进程启动方式**（setsid 双 fork；命令行禁含 `fundamentals_rebuild_manager`；ps 被禁用改用 pgrep）。
- **新增 §5.20**：`failed_stocks=0` 不等于零遗漏，要看 `source_missing.qfq_empty`。
- **§4.1 排除项**：test.py/test_baostock.py 已归位改为纳入；tmp/ 注明由 .gitignore 排除。
- M2 耗时 30 分钟 → 实测 14 分钟；F2 调用量写明 254,710。

## git 状态备忘
- main = 2f95fb4（PR #2 merge），feature = 75d160a（PR #1 merge），均与 origin 同步，远程已更新。
- 昨晚 23:00 推送曾因本地代理 `127.0.0.1:64367` 失效报 502，今早已随 PR 合并一并解决。
